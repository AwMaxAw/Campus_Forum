# Cloudflare Pages / Netlify / Vercel 通用重定向规则
# 语法（三列）：匹配路径  目标地址  HTTP 状态码
# 官方文档：
#   - Cloudflare Pages: https://developers.cloudflare.com/pages/configuration/redirects/
#   - Netlify:          https://docs.netlify.com/routing/redirects
#   - Vercel:           自动识别项目根 _redirects（也可用 vercel.json rewrites）
#
# 重要：本项目是纯静态站，发布目录就是仓库根，_redirects 直接放根目录即可。
#
# 说明：前端在 js/api.js 中已直接调用 Cloudflare Worker
#       (https://campus-forum.max-li-ggm.workers.dev)，
#       因此不再提供同源 /api 代理（Cloudflare Pages 也不允许 200 代理指向外部绝对 URL）。

# SPA 兜底：排除 index.html 自身（避免无限循环），其余所有路径回退到 index.html
/index.html   /index.html   200
/*            /index.html   200
