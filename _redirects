# Cloudflare Pages 重定向规则
# SPA 兜底：所有未匹配路径回退到 index.html
# Cloudflare Pages 会优先返回真实文件（index.html 存在于部署产物中），
# 因此 /* 规则不会命中 /index.html 自身，不会产生循环。
# 前端在 js/api.js 中已直接调用 Cloudflare Worker，无需同源代理。

/*  /index.html  200
